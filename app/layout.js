import './globals.css'
export const metadata = {
  title: 'Car Capitol | Search Every Car. See Your Real Payment.',
  description: 'Search millions of vehicles from every dealer and private seller. Get your real monthly payment with insurance, GAP & warranty included.',
}
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
